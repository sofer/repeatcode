<div id="header">
	<h1 class="left">
		<%= link_to 'Courses', courses_path %> |
		<%= @course.subject.name %> |
		<%= link_to 'lessons', course_path(@course) %> &amp;
		intervals
	</h1>
	<br clear="all">
</div>

<div id="main">
	<table>
		<tr>
			<% form_for @course do |f| %>
			<td>Revision</td>
			<td align="right">Interval</td>
			<td align="right">Accuracy*</td>
			<td align="right">
				Target: 
				<%= f.text_field :target %>
				%
			</td>
			<td align="right">
				<%= f.submit "Change" %>
				<% end %>
			</td>
	  </tr>

	<% for interval in @intervals %>
		<% responses = @responses[interval.index_no] %>
	  <tr>
	    <td><%=h ordinal(interval.index_no) %></td>
	    <td align="right"><%= interval_time interval.minutes %></td>
			<td align="right"><%= success_rate(responses) %></td>
			<td align="right"><%= formatted_date interval.updated_at %></td>
			<% if interval.index_no > 1 and responses[:correct] > 10 %>
			<td align="right">
				<% form_for interval do |f| %>
					<%= hidden_field_tag 'correct', responses[:correct] %>
					<%= hidden_field_tag 'incorrect', responses[:incorrect] %>
					<%= f.submit "Reset" %>
				<% end %>
			</td>
			<% end %>
	  </tr>
	<% end %>
	</table>
	<div class="right">
		<p>* since last reset</p>
	</div>
	<br clear="all">
	<div>
		<ul>
			<li>Each time you respond to an exercise, the <strong>Interval</strong> of time until your next <strong>Revision</strong> increases.</li>
			<li>The <strong>Accuracy</strong> of your responses at each interval gives you an indication of how close you are to your <strong>Target</strong>.</li>
			<li>An interval can be <strong>Reset</strong> in order to more closely match your target.</li>
			<li> You can also <strong>Change</strong> the default target (90%) at any time.</li>
		  <li>In this way, you can make best use of your time, and learn as thoroughly or as quickly as you like.
			</ul>
	</div>

</div>

<div id="footer">
	<div class="left">
		<br/>
		Logged in as: <%= current_user.login %>
	</div>
	<div class="right">
		<br/>
		<%= link_to "FAQ", "/about/faq" %> |
		<%= link_to "logout", "/logout"%>
	</div>
</div>
